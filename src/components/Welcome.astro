---
export interface Props {
	host: string;
	slogan?: string;
}

const { host, slogan } = Astro.props
---

<header>
  <div>
    <h1>Welcome to <span class="text-gradient">{host}</span></h1>

    {slogan && (
			<h3 class="text-gradient">
				{slogan}
			</h3>
		)}
  </div>

  <img src="favicon.png" alt="checkmark badge" />
</header>

<style>
	:root {
		--badge-size: 112px;
	}

  header {
    display: grid;
    grid-template-columns: 1fr var(--badge-size);
  }

	h1 {
		font-size: 3rem;
		font-weight: 800;
		margin: 0;
	}

	h3 {
		margin-bottom: 0;
	}

  img {
    max-width: var(--badge-size);
  }

	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
</style>
